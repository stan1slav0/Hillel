Завдання:

Запитуємо у користувача ім'я.
    Чистимо введене значення від пробілів на початку/кінці рядка. Наприклад, "Алла Вікторія" - допустиме, " Алла Вікторія " - не допустиме.
    Якщо користувач натичкає Cancel, або залишає пустий рядок, то робимо повторний запит ім'я.
Запитуємо у користувача прізвище.
    Чистимо введене значення від пробілів на початку/кінці рядка. Наприклад, "Шевченко-Франко" - допустиме, " Шевченко-Франко " - не допустиме.
    Якщо користувач натичкає Cancel, або залишає пустий рядок, то робимо повторний запит прізвища.
Запитуємо у користувача email.
    Чистимо введене значення від пробілів. Наприклад, "taras@gmail.com" - допустиме, " taras @gmail.com " - не допустиме.
    Приводимо введене значення до нижнього регістру.
    Робимо повторний запит email, якщо користувач:
        натичкає Cancel;
        залишає пустий рядок;
        введене значення не містить символ @
        введене значення містить символ @ не першому місці
        введене значення містить символ @ не останньому місці
Запитуємо у користувача рік народження.
    Чистимо введене значення від пробілів. Наприклад, "1990" - допустиме, " 19 90 " - не допустиме.
    Якщо користувач натичкає Cancel, або залишає пустий рядок, то робимо повторний запит року народження.
    
У змінну age підраховуємо актуальний вік користувача.
Робимо виведення даних користувача у body у вигляді списку <ul>. Приклад виведення:
Full name: Anna Ivanova
Email: annaivanova@gmail.com
Age: 55