Створюємо порожній масив, наприклад arr = [].
Запитуємо у користувача довжину масиву arr.
Мінімальна довжина масиву 2. Максимальна довжина масиву 10.
Якщо користувач вводить від'ємне значення, то за допомогою Math.abs() приводимо його до абсолютного.
Якщо користувач вводить не ціле число, то за допомогою Math.round() приводимо його до цілого.
Якщо користувач натискає Cancel або вводить значення, яке виходить за межі діапазону від 2 до 10 (включно), то продовжуємо запитувати валідне значення довжини.
Запитуємо у користувача мінімальне значення рандомного елементу масиву.
Мінімальне рандомне число має бути не більше 10 і не менше -10.
Якщо користувач вводить не ціле число, то за допомогою Math.round() приводимо його до цілого.
Якщо користувач натискає Cancel або вводить значення, яке виходить за межі діапазону від -10 до 10 (включно), то продовжуємо запитувати валідне значення.
Запитуємо у користувача максимальне значення рандомного елементу масиву.
Максимальне рандомне число має бути не більше 50 і не менше попередньо обраного мінімального значення.
Якщо користувач вводить не ціле число, то за допомогою Math.round() приводимо його до цілого.
Якщо користувач натискає Cancel або вводить значення, яке більше 50, або менше попередньо обраного мінімального значення, то продовжуємо запитувати валідне значення.
Заповнюємо масив arr рандомними числами у визначеному діапазоні.
Знаходимо мінімальний і максимальний елементи масиву. Виводимо їх в консоль.
Міняємо місцями в масиві мінімальний і максимальний елементи.